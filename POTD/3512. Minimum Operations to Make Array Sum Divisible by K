class Solution {
public:
    int minOperations(vector<int>& nums, int k) {
        
        int n=nums.size();

        int s=accumulate(nums.begin(),nums.end(),0);

        if(s<k) return s;
        if(s==k) return 0;
        
        return s % k;
    }
};